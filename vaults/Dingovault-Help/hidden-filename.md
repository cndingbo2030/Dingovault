---
title: Aliases Demo Page
alias: "[[Aliases Demo]]"
tags: [alias, demo]
status: active
---

# Aliases Demo

This page is stored as `hidden-filename.md` but the frontmatter **alias** lets wikilinks use `[[Aliases Demo]]`.

## Query example

Pages with `status: active` are discoverable via `ListPagesByProperty("status", "active")` from the app bridge.
